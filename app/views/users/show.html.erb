<div class="row">
  <div class="col-md-10">
    <h1><%= @user.name %><small> <%= @user.email %></small></h1>
    <h3 class="bg-primary text-center">TASKS</h3>
      <%= render partial: 'items/item', locals: {user: @user} %>
  </div>
</div>
<div class="row">
  <div class="col-md-6">
    <%= render partial: 'items/form', locals: {user: @user} %>
  </div>
</div>